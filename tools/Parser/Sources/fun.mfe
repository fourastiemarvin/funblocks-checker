(set include BOOL off .)
load funrules.maude
--- (select tool ChC .)
--- (cch FUNRULES .)
(select tool MTT .)
(select external tool aprove .)
(ct FUNRULES .)
--- (select tool CRC .)
--- (show cps .)
--- (ccr FUNRULES .)
--- (set include BOOL off .)
--- load inf.fm
--- (select tool ChC .)
--- (cch OvConsOS .)
--- (select tool MTT .)
--- (select external tool aprove .)
--- (ct OvConsOS .)
--- (select tool CRC .)
--- (ccr OvConsOS .)
