(set include BOOL off .)
load my-nat.maude
--- (select tool ChC .)
--- (cch FUNRULES .)

(select tool MTT .)
(select external tool aprove .)
(ct FUNRULES .)
(select tool CRC .)
(ccr FUNRULES .)
(show cps .)
